@model System.Data.DataTable

@{
    ViewData["Title"] = "List of Books";
}

<div class="jumbotron">
    <h1 class="text-center">@ViewData["Title"]</h1>
</div>

<table class="table table-bordered">
    <thead class="thead-light">
        <tr>
            <th>
                BookName
            </th>
            <th>
                Author
            </th>
            <th>
                ISBN
            </th>
            <th>
                Price
            </th>
            <th>
                publishedYear
            </th>
            <th>
                Publisher
            </th>
            <th>
                <a asp-action="AddOrEdit">Create New</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Rows.Count; i++)
        {

            <tr>
                <td>
                    @Model.Rows[i]["BookName"]
                </td>
                <td>
                    @Model.Rows[i]["Author"]
                </td>
                <td>
                    @Model.Rows[i]["ISBN"]
                </td>
                <td>
                    @Model.Rows[i]["Price"]
                </td>
                <td>
                    @Model.Rows[i]["publishedYear"]
                </td>
                <td>
                    @Model.Rows[i]["Publisher"]
                </td>
                <td>
                    <a asp-action="AddOrEdit" asp-route-id="@Model.Rows[i]["BookId"]">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@Model.Rows[i]["BookId"]">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
